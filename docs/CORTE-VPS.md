# Corte VPS · IE Brasil

**Canônico desde 2026-09-02:** VPS nova EasyPanel `se860g` · `65.109.139.4` · projeto `inteligencia-eleitoral-brasil`.

| | Valor |
|---|---|
| URL pública | `https://inteligencia-eleitoral-brasil-mcp-api.se860g.easypanel.host` |
| VPS antiga (`kxryyk` / `46.62.130.249`) | **mcp-api desligado**; Postgres só backup frio |
| Destruição Postgres antigo | **2026-09-09** (D+7) — não gravar / não consultar |

Radar/clima em `inteligencia-eleitora-painel.kxryyk` é **produto externo** (não é este banco).

Agentes: nunca apontar `DATABASE_URL` nem EasyPanel `kxryyk` para este produto.
